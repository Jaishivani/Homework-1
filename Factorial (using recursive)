#include <stdio.h>
int factorial (int);
void main()
{
    int n,fac;
    printf("Enter the number:");
    scanf("%d",&n);
    fac=fact(n);
    printf("The factorial is %d",fact(n));
}
int fact(int n)
{
    return (n*fact(n-1));
}
